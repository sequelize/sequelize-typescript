language: node_js

node_js:
  - "8"

before_install:
  - npm i -g npm@6

install:
  - npm install

script:
  - npm run lint
  - npm run build
  - export SEQ_SILENT=true
  - npm run cover

after_success:
 - 'nyc report --reporter=lcov > coverage.lcov && codecov'
